/**
 * DEPRECATED: Este arquivo está sendo gradualmente removido.
 * Todas as rotas foram migradas para src/routes/partner/
 */
import { Router } from 'express';

const router = Router();

// Todas as rotas deste arquivo foram migradas para src/routes/partner/appointments.routes.ts
// Este arquivo agora atua apenas como um placeholder.

export default router;
