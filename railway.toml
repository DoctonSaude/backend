# redeploy: 2026-04-04 (Forçando deploy manual)
[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh ./start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[healthcheck]
path = "/api/health"
httpMethod = "GET"
timeoutSeconds = 30
